{#
A config file for the LDAP client, with connection information for the local AD
server.
-#}
{% set output_path = '/etc/ldap/ldap.conf' %}
# LDAP Defaults
# See ldap.conf(5) for details

BASE           dc=lab,dc=shaka
URI            ldaps://localhost
TLS_REQCERT    never
